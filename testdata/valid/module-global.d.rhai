// This module extends a module that is already imported with a given name.
// Any module can be defined and extended this way, including `global`.
//
// All definitions are available via `<module-name>::foo`.
//
// Cannot contain operator definitions.
module my_module;

